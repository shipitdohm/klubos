{
  "name": "clubos",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "sync:assets": "mkdir -p public/css public/js public/legacy && cp css/style.css public/css/ && cp js/app.js public/js/ && cp mr8as03a-clubos_logo_white_transparent.png public/ && cp *.html public/legacy/",
    "dev": "npm run sync:assets && astro dev",
    "build": "npm run sync:assets && astro build",
    "preview": "astro preview",
    "check": "astro check"
  },
  "dependencies": {
    "astro": "^5.7.10"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "typescript": "^5.8.3"
  }
}
